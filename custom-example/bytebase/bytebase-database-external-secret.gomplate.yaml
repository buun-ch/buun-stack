apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bytebase-database-external-secret
  namespace: {{ .Env.BYTEBASE_NAMESPACE }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-secret-store
    kind: ClusterSecretStore
  target:
    name: bytebase-database-secret
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        username: "{{ `{{ .username }}` }}"
        password: "{{ `{{ .password }}` }}"
  data:
    - secretKey: username
      remoteRef:
        key: bytebase/database
        property: username
    - secretKey: password
      remoteRef:
        key: bytebase/database
        property: password